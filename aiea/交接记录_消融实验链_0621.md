# 交接记录（给新会话续接用）— 2026-06-21 上午

> 用途：当前会话已压缩320次变慢，用户将手动 new 开新会话。本文件是续接载体，新会话读这份+下面"任务清单"即可无缝接着干。
> 工作目录：/home/student/arthas/mentalDistill

---

## 🎯 当前最高优先任务：消融实验链 + 汇总小论文

**用户要的最终交付物**：把所有消融实验（α消融已完成 + 7B消融待做 + Llama消融待做）的结论与分析，汇总成一篇给导师看的小论文，让导师理解实验全貌。**这篇是给导师的独立报告，不是给AIEA的**（AIEA已过会议定稿期，不能改了）。

### 消融链进度
1. **14B α消融——已完成** ✅
   - 结果：最优α=0（纯CE/SFT），KL权重越高越差，纯KL(α=1.0)最差。
   - canonical eval（论文口径）：α=0 full=**89.14%** > 主结果α=0.35的88.67% > 教师87.18%。
   - 结论：超越教师成立，但靠"决策空间监督(Choice-Head)"，非dark knowledge蒸馏。落在方案"情景B"。
   - 文档：`aiea/alpha_ablation_results.md`（已含21点表+canonical结果+三层结论分析）。
   - 脚本：`15_fulldata_resplit/scripts/run_alpha_ablation.sh`（幂等）。

2. **7B α消融——待做（用户已确认要做）**
   - 目的：验证最优α是否随学生容量变化（理论预期7B弱学生最优α可能>0，因弱学生更需软标签正则）。
   - 做法：复用 run_alpha_ablation 模式，改学生为 Qwen2.5-7B，同7α×3seed网格，仅Stage-1。
   - **统一用 canonical eval**（与14B同口径，evaluate_model.py + full991/dental125）。
   - 注意：7B主实验种子是11/42/7（不是8），但消融为与14B对齐用11/42/8亦可——按方案2.2"只跑Stage-1对齐"。

3. **Llama消融——待做（接在7B后，用户确认）**
   - 背景：用户以前有个Llama实验也做到了学生超越老师，目录线索：`16_llama70b_choice_head/`（有自己的run_eval_dual.py）。
   - 做法：对该实验做同样的α消融，**统一canonical eval**。
   - 先去 `16_llama70b_choice_head/` 摸清它的教师/学生/数据/脚本结构再动手。

4. **汇总小论文——最终交付**
   - 把14B + 7B + Llama三组消融的结论+分析+理论解释，写成给导师的小论文。
   - 核心叙事：跨"学生容量(7B/14B)+不同师生组合(Llama)"验证"最优α"规律 + 决策空间监督机制 + 诚实归因(非dark knowledge)。
   - 理论框架（已分析，写论文用）：最优α取决于①教师-金标准不一致率(14B这里12.2%)②任务类别数(5选1→软标签信息少→推向α=0)③学生容量(强学生更不需KL)。预期"小决策空间+强学生+低教师噪声"组合α≈0稳健；弱学生/大词表/弱教师时α可能右移。

---

## ⚙️ 后台流水线状态（全部setsid守护，开新会话不影响）

**串行链（单H100顺序，靠 *_DONE 标记接力 + gpu_busy 检测避免抢卡）**：
- 任务3 MedQA蒸馏：进行中，已完成 Phi4×3seed + Yi34B seed42，当前跑 **Yi34B seed8**。剩 Yi34B s11 + Qwen32B×3。约每run 2小时。
- 泛化验证(Qwen7B体检)：等 TASK3_DONE。
- D2容量曲线(5学生尺寸)：等 GENVAL_DONE。
- P3补教师(GLM/Gemma/Qwen14B × medqa/mmlu_med/mmlu_full logprobs)：等 D2_DONE。脚本 `p3_extend_teachers.sh`，已挂。

**关键脚本**：
- 切换/插队编排范式：`canonical_eval_alpha0.sh`（已完成，可参考它的"等当前run完成→pkill暂停→插队→恢复relay"模式来插7B/Llama消融）。
- 恢复用：`task3_medqa_relay.sh`（幂等，pkill后重启自动续）。

---

## 📋 新会话第一步建议

1. 读本文件 + `aiea/alpha_ablation_results.md` + 任务清单(todo)。
2. 搭 **7B α消融脚本**（仿run_alpha_ablation改学生路径），用插队范式排进流水线（参考canonical_eval_alpha0.sh的暂停/恢复逻辑），或等任务3链跑完再跑——**询问用户要插队还是垫底**。
3. 7B消融的canonical eval一并自动跑。
4. 7B完成→摸 `16_llama70b_choice_head/` 结构→Llama消融。
5. 三组齐了→写汇总小论文给导师。

---

## 🚨 工程踩坑（已入memory，新会话注意）
- 写 math/ 文档用完整相对路径 `math/x.md`（cwd=仓库根），write_file的path不受terminal cd影响，曾误落 math/math/、math/research/，需 ls 检查、必要时 mv 移正。
- 破坏性删除(rm -rf)用户会拒绝agent发起——列出命令让用户自己跑。
- 单H100只有1张卡，GPU任务必须串行，14B训练占~50GB，别并行。
- 读训练log别整个读（会刷屏几十万字符），用 `tail -c` 或 grep tqdm。
- 后台进程启动用 background=true（Hermes跟踪），不要前台nohup（会被拦）。

---

## 一句话
**消融链：14B α消融已完成(α=0最优, canonical full=89.14%>教师, 决策空间监督而非蒸馏)。待做：7B消融→Llama消融(均统一canonical eval)→汇总成给导师的小论文。后台任务3/泛化/D2/P3串行跑中(setsid守护)。AIEA已定稿不可改, 小论文是给导师看全貌的独立报告。**
