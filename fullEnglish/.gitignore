# fullEnglish — 只忽略「可重新生成的产物」，保留代码/配置/文档/结果报告/原始数据。

# ── 数据切分产物（由 00_data/build_data.py 重新生成）──
00_data/out/

# ── 教师 logprobs 与 API 标签（由筛选/标注脚本重新生成）──
01_teacher_screening/logprobs/
01_teacher_screening/labels/

# ── 主蒸馏：教师标签 + 训练数据 + 训练产物 ──
03_main_distill/labels/
03_main_distill/data/
03_main_distill/runs/

# ── 图表 PNG（由 figures/make_figures.py 重新生成）──
figures/*.png

# ── Python 缓存 ──
__pycache__/
*.pyc
