# 仓库提交方案：让别人 clone 就能复现整个实验

> 日期：2026-06-20
> 目标：提交到远端 Git 仓库，别人 clone 后能复现整个实验过程。只提交必需文件，大模型/过程文件不提交，但用文档详述如何获取。
> 本方案需用户确认后执行（涉及"提交什么/不提交什么"的判断）。

---

## 1. 当前仓库状态（已盘点，基本健康）

- 已跟踪文件：435 个，主要是代码 + 文档 + 小数据。
- ✅ 大模型 `models/`、训练产物 `**/runs/`、`**/outputs/`、`**/logs/` 都已被 .gitignore 排除，未跟踪。
- ✅ 大 jsonl（teacher_*、train_head_distill）已忽略。
- ⚠️ `data_ext/`（21M，新下载的 MMLU/MedQA）未忽略，**待决策**。
- 历史最大文件：几个 PDF 参考文献（thesis/reference/，最大 19M）、cmexam_full.csv（5.8M）。不算大，但要决定是否保留。

---

## 2. 需要你决策的 3 个点

### 决策1：data_ext/（下载的公开数据集，21M）
- **建议：不提交**，改为提供下载脚本 `load_mcq_dataset.py`（已有）+ 文档说明。
- 理由：MMLU/MedQA 是公开数据集，任何人能用脚本重新下载；提交原始数据既占空间又可能有许可问题。
- 例外：CMExam 若是你论文的核心私有数据，可考虑提交（见决策2）。

### 决策2：CMExam 数据（15_fulldata_resplit/data/，train.jsonl 4.8M）
- 当前 train.jsonl 等已被跟踪（在 git 历史里）。
- **问题**：CMExam 有使用许可吗？能公开吗？
  - 若可公开 → 保留（别人能直接复现）。
  - 若不可公开 → 应移除并改为说明"从 CMExam 官方申请获取"。
- **需要你告诉我 CMExam 的许可情况。**

### 决策3：thesis/reference/ 下的 PDF 参考文献（共约 50M+）
- 这些是你读的论文 PDF，受版权保护。
- **建议：不提交**，改为在文档里列参考文献清单（标题+arXiv链接）。
- 需从 git 历史移除（否则 clone 仍会拉取）。

---

## 3. 计划新增的复现文档（核心交付）

### 3.1 顶层 README.md（仓库门面）
- 项目一句话简介
- 环境依赖（Python版本、关键库、requirements.txt）
- 目录结构说明（每个目录是干什么的）
- 快速开始：3 步跑通最小实验
- 指向详细文档

### 3.2 docs/复现指南.md（详细步骤）
内容：
1. **环境搭建**：conda 环境、pip install、setup.env 怎么配（给模板 setup.env.example）。
2. **模型下载**：6 个教师 + 学生模型，每个的 HuggingFace repo、下载命令（hf download / hf-mirror）、存放路径。
3. **数据准备**：
   - CMExam：如何获取（取决于决策2）。
   - MMLU/MedQA：`python load_mcq_dataset.py --dataset xxx`（已有脚本）。
4. **实验清单**（每个实验：目的 / 启动命令 / 产出 / 预期结果）：
   - 教师 logprobs 生成
   - 教师几何分析 + 可蒸馏性打分
   - LEEP/LogME 迁移性打分
   - 3-seed 几何筛选蒸馏（CMExam）
   - H1 指标家族对比
   - 跨数据集验证（MedQA）
5. **结果文件位置**：每个实验产出在哪、怎么读。
6. **依赖的随机种子 / 复现注意事项**。

### 3.3 docs/实验脚本索引.md
- 列出 `research/distillability/scripts/` 下每个脚本：名字 / 作用 / 用法 / 依赖。

---

## 4. 你的研究 md 文档（全部保留）

`math/` 下所有 .md 是你的研究记录，**全部保留并提交**。包括：
- 方向B系列（结果、查重、价值评估、数据集扩展、定位转向）
- 严谨性总纲、可质疑点清单、大白话解释、研究全景
- 写作笔记、实验报告滚动更新

这些是你研究过程的真实记录，是复现"思路"的关键。

---

## 5. .gitignore 需补充的条目

```
# 下载的公开数据集（用 load_mcq_dataset.py 重新生成）
data_ext/

# 扩展实验的教师 logprobs（大，可重新生成）
research/distillability/teacher_labels_ext/

# 参考文献 PDF（版权，不提交）—— 若决定移除
thesis/reference/*.pdf
```

---

## 6. 执行步骤（你确认后我来做）

1. 根据决策1/2/3 更新 .gitignore。
2. 写 README.md + docs/复现指南.md + docs/实验脚本索引.md + setup.env.example。
3. 若需移除已跟踪的大文件/PDF：`git rm --cached`（保留本地，仅从仓库移除）。
4. 整理 commit（按你的 git 习惯，不擅自 push）。
5. 验证：模拟"干净 clone"检查文档是否足够复现。

---

## 7. 待你回答的问题

1. **CMExam 数据能公开提交吗？**（决策2）
2. **thesis/reference 的 PDF 要从仓库移除吗？**（决策3，建议移除）
3. **远端仓库是新建还是已有？**（影响是否要处理历史大文件）
4. **data_ext 确认不提交、只留下载脚本？**（决策1，建议是）
