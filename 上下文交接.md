# 上下文交接（会话刷新后恢复用）

> 刷新会话后，先读本文件 + 工作记录.md，即可恢复全部状态继续推进。
> 最后更新：2026-08-17 20:35（用户即将 /new 切换会话）

---

## 一、项目与目标（一句话）

fullEnglish 实验：检验「英文全科医学选择题」上，学生能否超越教师（DeepSeek 等）。
方法 = Choice-Head 蒸馏（α=0 纯 GT SFT）。核心机制已坐实：**增益≈4pp 恒定，能否超越只看 headroom（老师零样本−学生零样本）是否 < 增益**。

**当前主线（2026-08-17 收窄后的目标）**：
1. 【高优先】找「学生+老师都 ≥78%、学生可部署 H100、两者零样本相差 ≤4%」的组合。已穷尽现有模型：可部署非思考学生天花板 73.84%（Qwen3-32B），无 76% 档，故"找组合"卡死 → 唯一路是 PED 提增益。
2. 【低优先→已升为关键】PED 精准纠错蒸馏：把"差一点答对"题的纠正率从 1/3 往上提，增益 4pp→6pp，让 Qwen3 训练后 73.84+6=79.84% 超越 flash 79.80%，命中 78% 目标。

---

## 二、会话约定（用户偏好，强制遵守）

1. 屏幕输出精简，命令输出严禁刷屏（重定向/`| tail -3`），详细写文件。
2. 英文翻译中文。
3. **答复一律用大白话**（用户明确反馈：术语、公式、代码连用户都看不懂，更别说老师）。凡用术语必先用生活比方解释。
4. **正确率以全量 8293 题口径为准**（600 题只是初筛/补充锚，明确标注）。
5. 重要结论落盘 md（工作记录.md 技术版 + 汇报材料_给老师.md 大白话版），不能只靠终端输出。
6. 不贴代码/JSON 到回复里（刷屏）。

---

## 三、已完成的关键结论（全部）

### 1. 核心机制（最重要）
- **蒸馏增益 ≈4pp 恒定**，与学生规模/国籍/语言基本无关：中文 5.12 / 英文牙科 4.96 / 32B 4.26 / 14B ~4 / Llama70B 3.84 / Qwen3-32B 3.75。
- **能否超越只看 headroom**：headroom < 增益→超越；headroom > 增益→不超越。
- **α=0 最优**（KL 越多越差），教师软标签不参与 → 天然规避蒸馏 ToS。

### 2. 四学生单调线（全量 8293 口径，论文核心证据）
| 学生 | 零样本 | 训练后 | 增益 | Δ vs flash 79.80% |
|---|---|---|---|---|
| Qwen2.5-14B | — | 72.41 | ~4 | −7.39 |
| Qwen2.5-32B | 71.38 | 75.64 | +4.26 | −4.16 |
| Llama70B | 72.43 | 76.27 | +3.84 | −3.53 |
| Qwen3-32B | 73.84 | 77.60 | +3.75 | −2.21（最接近）|

### 3. 教师锚（600 题筛选口径，仅作补充；关键锚 flash 是全量）
- 豆包 turbo 90.82% / GLM-5.2 90.54% / deepseek-v4-pro 90.33%（新一代推理模型 ~90%，高度一致）
- Qwen3-32B thinking 82.17%（可部署，但已放弃当学生）
- **flash 79.80%（全量）是主教师锚**；pro 比 flash 高 ~8.66pp（600 口径）
- **主教师一直是 deepseek-v4-flash，不是 pro**（实测 deepseek-chat → v4-flash）

### 4. 增益 ≈4pp 的根因（量化规律，已定位到八成）
- 增益 = "差点答对"的题（正确答案是零样本第 2 位）× 纠正率(~1/3)。
- 实测：top2−top1 ≈ 12~15%（600 题 logprobs；训练集约 15.5%），但增益只有 ~4pp → SFT 只翻动约 1/3（"二选一犹豫、把握接近"的题）。
- 判断题 +11pp 反证：零样本 48.6% 接近瞎猜，"差点答对"空间巨大 × 1/3 ≈ 10pp。
- **消融实锤**：rank64 过拟合(70.36% val)、rank128 发散、epoch3 饱和(73.51) → 加容量/加训练量都没用，4pp 是 α=0 GT SFT 的硬天花板，翻动率 1/3 提不动。
- 详见 `fullEnglish/4%增益的来龙去脉_大白话.md`（给老师的大白话版）。

### 5. 已实现的"学生超越老师"（弱老师场景）
- 老师 = Qwen3-32B 零样本 73.84%：Qwen2.5-32B 训练后 75.64%（超 +1.80）、Llama70B 76.27%（超 +2.43）。
- 机制"相变"：headroom<4pp 就超（Qwen3 当老师）、>4pp 就不超（flash/pro/GLM/豆包当老师）。

### 6. 其他
- 判断题泛化 +11.17pp（48.6→59.77，证明迁移推理非背题）。
- R1-Distill-Qwen-32B：65.17%（弱学生，不训练）；CoT 模型英文 prompt 吐推理链、中文 prompt 92% 直接作答。
- Qwen3-32B 之前 27.54% 是评估脚本 bug（Qwen2.5 硬编码模板 + 没关 thinking），修正后真实 73.84%。`train_choice_head_distill.py` 已加 `enable_thinking=False` 条件修复。

---

## 四、当前后台任务状态（2026-08-17 20:35）

**一个后台进程在跑：**
- **proc_b2c34f527f08 = PED 手段① 选题**（ped_select_almostright.py）：算 Qwen3-32B 在训练集 20488 题的零样本分布，筛"差点答对"的题。已到 ~2000/20488（差点答对约 15.5%），**ETA ~22:30 完成**。产出：
  - `fullEnglish/03_main_distill/data/qwen3_train_logprobs.jsonl`（全量 logprobs，供 top-k 分析）
  - `fullEnglish/03_main_distill/data/train_head_almostright.jsonl`（选题训练集）

---

## 五、恢复后的行动清单（按顺序）

1. `process(action="list")` 查后台进程，看 PED 选题是否完成。
2. **PED 选题完成后**：看"差点答对"筛出多少题（预期 ~15% = ~3000 题）。
3. **PED 手段① 训练**：用 `train_head_almostright.jsonl` 训练 Qwen3-32B（α=0, rank16, 1epoch，DISTILL_USE_CHAT_TEMPLATE=1）。可直接复用 `run_qwen3_student.sh` 改 data_path，或写个新脚本。
4. **PED 手段① 评估**：用 `eval_qwen3.py` 评估训练后 adapter，看增益是否从 3.75pp 往上走（目标 6pp → 训练后 79.84% 超越 flash）。
5. 若增益明显提升 → 命中 78% 目标；若无效 → 上 PED 手段②（教推理，train_rationale_sft.py，需先生成 train_cot.jsonl）。
6. 可选：补测 pro/GLM/豆包全量（API，慢，不影响结论）。

---

## 六、关键文件位置

- `工作记录.md`（根目录，技术日志，全部数字+过程）
- `fullEnglish/汇报材料_给老师.md`（大白话汇报，含第九节"今晚新结果"）
- `fullEnglish/4%增益的来龙去脉_大白话.md`（4% 根因，给老师的大白话版）
- `fullEnglish/PED精准纠错蒸馏方案.md`（PED 独立方案）
- `fullEnglish/零样本阶梯与4%组合.md`（任务 3：同口径零样本表 + 4% 对分析）
- `fullEnglish/增益常数分析.md`（4% 机制的技术版假说）
- `fullEnglish/模型组合清单.md`、`fullEnglish/主实验结果.md`、`fullEnglish/美国学生_教师组合分析.md`
- 结果文件（runs/）：eval_results_qwen3.json（Qwen3 学生 3seed）/ eval_results_qwen3_zeroshot.json（73.84%）/ eval_results_qwen3_thinking_600.json（82.17%）/ eval_results_llama70b.json / eval_results_14b.json / mmlu_subject_analysis.json / medmcqa_subject_analysis.json
- 关键脚本（03_main_distill/）：eval_qwen3.py / eval_qwen3_thinking.py / ped_select_almostright.py / run_qwen3_student.sh / run_ablation.sh / eval_r1distill_zeroshot.py
- API key：setup.env（gitignored）。DOUBAO_API_KEY 已填（ark），可调豆包/GLM/deepseek 系列。账户已充值。

---

## 七、环境

- Python：`$HOME/anaconda3/bin/python3`（torch 2.9.1+cu128 / transformers 4.57.6 / peft 0.7.1，GPU H100 95GB）
- 模型：Qwen2.5-32B / Qwen2.5-14B / Qwen3-32B / Llama-70B(原始) / Gemma-3-27B / DeepSeek-R1-Distill-Qwen-32B / Qwen2.5-0.5B / phi-4
- 训练脚本支持 `DISTILL_USE_CHAT_TEMPLATE=1`（chat template）+ 动态 padding + `--quantize 4bit`（QLoRA）
- **Qwen3 关键坑**：必须 `DISTILL_USE_CHAT_TEMPLATE=1` + `enable_thinking=False`（train_choice_head_distill.py 已自动处理），否则触发 thinking 输出推理链被截断 → 结果全废。

---

## 八、当前主线判断（给新会话的一句话）

**"学生超越老师"已实现（弱老师 Qwen3 场景，+1.8/+2.4pp），但"双 78% + 4% 差"的强组合卡死在"没有 76% 档的可部署学生"。唯一破局路 = PED 提增益（4pp→6pp，让 Qwen3 训练后 79.84% 超越 flash）。PED 手段①（精准选题训练）正在跑选题，跑完就训练验证。**
