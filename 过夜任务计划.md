# 过夜任务计划（2026-08-16 晚 → 08-17 早 7 点）

> 目标：明早 7 点前把能跑的尽量排满，产出「多教师 headroom 表」+「更强学生 Qwen3 的真实 headroom（可能启动训练）」+ 完整汇报。
> 更新：2026-08-16 19:45

---

## 〇、今晚发现并处理的问题（3 个）

1. ~~Qwen3-32B 下错版本~~ **已澄清**：`Qwen3-32B-Instruct` 在 ModelScope 不存在（404），`Qwen3-32B` 本身就是 instruct。27.54% 垃圾是**评估脚本模板 bug**（Qwen2.5 硬编码 + eos 错 + 没关 thinking），已改 `apply_chat_template(enable_thinking=False)`，冒烟 5/5 全对，重新评估中。
2. **豆包 turbo 卡住**：读超时（120s），已重启改 timeout=300。
3. **R1-Distill**：CoT 模型，英文 prompt 吐推理链、中文 prompt 92% 直接作答；600 题零样本 **65.17%**（弱于 Qwen2.5-32B 学生 72.67），不值得训练。

---

## 一、GPU 串行队列（H100 一张，全程不闲置）

| 时间(约) | 任务 | 时长 | 产出 |
|---|---|---|---|
| 19:45 | ~~R1-Distill 零样本~~ **跳过**（推理链模型，强 prompt 也压不住 CoT，需全链生成 ~29h） | — | — |
| 20:15 | Qwen3-32B-**Instruct** 零样本评估（下载完即可跑，先冒烟验 thinking 关没关） | ~1-1.5h | `eval_results_qwen3_instruct_zeroshot.json` |
| ~21:45 | **判定**：Instruct 零样本 headroom 是否 < 7.37pp(Llama70B) | — | 决定是否训练 |
| ~21:45 | 若达标 → Qwen3-32B-Instruct 训练（Choice-Head 蒸馏 α=0 × 3 seed） | ~2.5-4h/seed ≈ 7.5-12h | 3 seed adapter |
| 若未达标 | 改跑「学习预算消融」（LoRA rank 16/64/128、epoch 1/3，用现有 Qwen2.5-32B） | ~8h | 增益常数机制证据 |

> 关键前置：Qwen3-Instruct 有 thinking/non-thinking 双模式，训练/评估前要先冒烟验证 chat template 正确、且关掉 thinking（否则会吐推理 token、max_new_tokens 截断）。这是 Qwen3 训练最大的不确定性。

---

## 二、API 并行队列（不占 GPU，整夜）

| 任务 | 模型 | 状态 |
|---|---|---|
| 豆包 600 题 | doubao-seed-2-1-turbo-260628 | 重启中（改 1.5s 间隔 + 300s 超时） |
| DeepSeek-v4-pro 600 题 | deepseek-v4-pro-260425 | 跑着（50/600） |
| GLM-5.2 600 题 | glm-5-2-260617 | 跑着（14/600） |

完成后统一 `aggregate_screening.py` → 6 教师 headroom 表（DeepSeekV3=flash 81.67% / Llama70B 79.50% / 学生 72.67% / 豆包 / DS-v4-pro / GLM-5.2）。

---

## 三、CPU/分析队列（穿插，随时做）

1. 多教师 600 题聚合 → headroom 表（API 评测完成后）。
2. Qwen3-Instruct 训练脚本准备（chat template + 关 thinking + 冒烟）。
3. R1-Distill 零样本评估脚本编写。
4. **明早 7 点汇报材料**：完整结论 + 过夜新结果，落盘 `汇报材料_给老师.md`（大白话）+ `工作记录.md`（技术版）。

---

## 四、明早 7 点汇报里会有的东西

- 多教师 headroom 表（6 个教师：flash/pro/豆包/GLM/Llama + 学生基线）。
- Qwen3-32B-Instruct 真实零样本 headroom（决定 Qwen3 是否值得训练）。
- R1-Distill 零样本。
- 若时间够：Qwen3 训练的首个 seed 结果 / 学习预算消融首结果。
- 关键澄清：主教师一直是 deepseek-v4-flash 不是 pro；换 pro 只会 headroom 更大、结论更硬。

---

## 五、风险与依赖

- Qwen3-Instruct 训练需要适配（thinking 模式），可能比 2.5 系列慢/踩坑，最坏情况训练推迟到明天白天。
- **R1-Distill 零样本已确认今晚不可行**：CoT 模型强 prompt 压不住推理链（冒烟实测），需全链生成（~29h），延后到有预算时用「generate 到 <|im_end|>」或 logprobs 方案。
- 豆包 turbo 卡住根因已定位 = 读超时（120s，难题推理过久），已重启改 timeout=300 + 间隔 1.5s；若再卡则降级 seed-2.0-lite。
- API 三个评测是同一 ark 账号，可能互相触发账号级限流（目前 DS-pro/GLM 正常，说明不是全局限流）。
